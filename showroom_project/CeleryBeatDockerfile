FROM python:3.10

WORKDIR /showroom


COPY ./ /showroom
RUN pip install --upgrade pip

RUN pip install -r requirements.txt
RUN pip install celery


CMD ["celery", "-A", "showroom_project", "beat", "-l", "info"]